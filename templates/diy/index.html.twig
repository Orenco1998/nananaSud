{% extends 'base.html.twig' %}

{% block title 'Tous nos diy' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            {{ form_start(form) }}
            <div class="form-row align-items-end">
                <div class="col">
                    {{ form_row(form.title) }}
                </div>
                <div class="col">
                    {{ form_row(form.link) }}
                </div>
                <div class="col">
                    <div class="form-group">
                        <button class="btn btn-success">Rechercher</button>
                    </div>

                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
    <div class="container mt-4">
        <h1>Nos diy</h1>

        <div class="row">

            {% for diy in diys %}
                <div class="col-md-4">
                    {% include 'diy/_diy.html.twig' %}
                </div>

            {% endfor %}
        </div>

        <div class="pagination">
            {{ knp_pagination_render(diys) }}
        </div>
    </div>
{% endblock %}